v3.2.8：AI 现在能陪你聊了（自由对话 + 导出 TXT），并修好光标、弹层与小组件的一批体验问题。

【AI 自由对话】右键「AI 处理 ▸」最后一项新增「和 AI 聊聊…」：选中的那条记录直接作为上下文，你想问什么就问什么，多轮问答、流式显示（能看着字出来）。打开时不会自动发请求，你按下发送才开始；多轮只带最近 6 轮，token 不会越滚越多。「重新生成」= 重发上一条提问并替换它的回答；结果照旧可以复制、另存为新条或替换本条。

【导出 TXT】AI 弹层新增「导出 TXT」：动作模式导出结果本身，对话模式导出整段对话（你：… / AI：…），和「文本」分类的导出同一套做法，导出后弹卡片提示。

【服务商预设校正】按 DeepSeek 官方「模型 & 价格」页把内置预设改成 BASE URL https://api.deepseek.com、模型 id deepseek-flash 与 deepseek-v4-pro（原来的 deepseek-v4-flash 是文档里的旧写法）。通义 / 智谱 / OpenAI / Ollama 的地址也逐个实测过（返回 401 说明路径正确、只是 Key 无效），地址仍然可以手动改。

【光标】小组件上光标刚变成缩放箭头就被"兜底刷新"清回箭头——现在兜底逻辑跳过小组件自己的窗口；设置里「打开同步窗口」这类从没设过光标的按钮，以前只有从下往上扫过才变手型——现在窗口一打开就统一补齐手型，并按鼠标位置每 150ms 兜底（鼠标下方是可点控件时强制手型，离开或关闭自动撤销）。

【弹层与小组件】打开设置 / AI 配置 / 历史保留策略时，桌面小组件依然可以点、可以复制（窗口级模态只挡住宿主窗口）；主窗口最大化时全屏遮罩曾把小组件盖住——现在弹层显示后会把小组件重新抬到最上层。卡片弹层不再"始终置顶"，切到别的程序时会跟着程序到后面，不会孤零零飘在浏览器上。另外，AI 对话输入问题后不会再直接出现「已停止生成」（原因是发送时禁用了输入框、焦点跳到「停止」按钮，再按一次回车等于点了停止；现在输入框保持可用、按钮不接受键盘焦点，显式点停止仍然有效）。

【设置窗口】默认尺寸收敛为 480×660（英文 600×660），限制在屏幕可用区之内，最小可缩到 420×420。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
