v3.2.4：修掉"光标被锁成缩放箭头"，更新过程不再闪终端，文件分类更细，安装版卸载时会问一次要不要保留数据。

光标锁定：贴着主窗口 / 桌面小组件的边缘缩放一次之后，光标会一直卡成缩放箭头（"缩放 → 打开设置 → 关掉设置"之后必现）。根因是设置窗口里每 120ms 跑的"光标兜底刷新"，它把继承来的缩放光标复制成了子控件自己的光标，之后父窗口再怎么按位置重算都改不动它。现在子控件也上报鼠标移动、松手按真实位置重算、鼠标进入窗口时重算、关掉设置后立刻重算，并且那个兜底刷新不再复制继承光标、还会把已经被误钉上的缩放光标清掉。实测直接读 Windows 系统光标：贴边 = 缩放箭头，中间 / 表格上 = 普通箭头。

更新过程不再闪终端：更新脚本原来用 timeout.exe 做等待，在无控制台环境里它起不来、也不等待，重试会瞬间连发；现在改用 ping 真正等待，重试上限 60 次，并用隐藏控制台方式启动——实测整个更新过程 0 个可见控制台窗口。

文件标签按类型细分：「文件」标签新增一排筛选按钮（全部 / 视频 / 图片 / 设计源文件 / 音频 / 文档 / 压缩包 / 程序 / 代码 / 字体 / 其他，带数量），每行文件名前显示所属细分，共覆盖 403 个扩展名（mp4→视频、psd/ai→设计源文件、wav/mp3→音频、rar/7z→压缩包…）。「全部」标签里这些记录依旧是"文件"，不受影响。

安装版卸载时会问一次"是否同时删除本地数据"：默认「否」= 保留（历史、图片、设置、加密密钥都留下，重装后继续读取）；选「是」才会一并删除；静默卸载按默认保留走，不会误删数据。

如果你还在 v3.2.3 或更早版本，升级还会一并拿到：更新时清掉继承的 PyInstaller 环境变量（不再出现 Security validation failure / Failed to load Python DLL 而必须重开）、设置界面分组感重做、列表里 Ctrl+C 复制完整原文、预览面板完整显示长文、大小单位补齐到 GB / TB、复制文件不再多出一条"文件名文本"、窗口缩放更顺滑且背景保持比例铺满、退出不残留临时目录、中英文界面不再互相夹杂。构建脚本里的 PyInstaller 固定为 6.22.3。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
