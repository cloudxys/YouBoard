v3.2.6：这版是"工程化"更新——给发版加了自动门禁，并把版本号收敛成单一来源（功能与界面没有变化）。

发版门禁：回归测试脚本搬进仓库（tools/verify_all.py，路径按脚本位置推导，本机与 CI 通用），CI 在打包之前先跑一遍，不通过就直接中断，不会产出、也不会发布有问题的安装包。

版本号单一来源：新增 youboard_version.py 作为唯一真源，配合 tools/sync_version.py 生成 EXE 版本资源（version_info.txt）与安装包版本定义（version_defines.iss）；程序本体、macOS 包版本、DMG 名、CI 产物名都直接读它。以前改一次版本要动 8 个文件、39 处字符串（还容易打错漏改），现在只改 youboard_version.py 一处，再跑一次 python tools/sync_version.py --docs 即可，文档里的下载文件名也会一起更新。

README 顶部新增一排徽章（最新版本 / 下载量 / 支持平台 / License / Star），由 shields.io 动态生成，发新版后版本号徽章自动跟着变。

如果你还在 v3.2.5 或更早版本，升级还会一并拿到：修掉"鼠标从上往下扫过按钮时手型光标不出现"、可点控件统一手型光标、自定义皮肤与历史保留策略统一卡片风格、置顶记录带主题色「置顶」胶囊、历史保留策略的自定义行改成小时/天胶囊；更早版本还包含光标不再被锁在缩放箭头、更新过程不闪终端、文件按类型细分（403 个扩展名）、安装版卸载可选择保留数据等。构建脚本里的 PyInstaller 固定为 6.22.3。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
