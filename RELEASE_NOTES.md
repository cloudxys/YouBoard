v3.2.5：修掉"鼠标从上往下扫过按钮时手型光标不出现"，并把可点控件的光标、两个弹层的风格、置顶标记统一起来。

光标方向问题：设置窗口里鼠标自上而下经过按钮时光标不变手型，只有反向（自下而上）才有。根因是那个"光标兜底刷新"依赖 QApplication.widgetAt，而它对本应用自己的无边框 / 半透明窗口经常取不到控件（返回 None），整条兜底就失效了。现在改成沿窗口自己的控件树用 childAt 查找（多窗口重叠时优先对话框、再取面积最小的那个），并向上归到最近的可点控件，方向不再影响结果。

可点控件光标统一：之前设置里的几个开关、以及「选择背景图片」「恢复默认」「复制链接」「刷新二维码」「关闭」这类按钮没有手型光标，现在统一补齐（只在控件可用、可见时生效）。

「自定义皮肤」与「历史保留策略」现在共用同一套卡片弹层（同一个基类）：同样的圆角卡片、顶部圆形图标、标题 / 副标题层级、右下「取消 + 保存」按钮；自定义皮肤的四个颜色行也用同一套控件规格（圆角、内边距一致），色值文字按底色自动切换黑 / 白。

置顶的记录更好认：列表里被置顶的行，内容前会画一个主题色「置顶」小胶囊，任何分类里都能一眼看到（行底色仍是原来的浅色调，未改配色）。

「历史保留策略」的自定义行重做：原来的「小时 / 天」下拉框有直角外框，靠近窗口底部时还会向上翻、盖住下面的说明；现在换成两个小胶囊按钮，和上面那排选项同款，切换单位时输入上限跟着变（小时 1–8760 / 天 1–365）。

如果你还在 v3.2.4 或更早版本，升级还会一并拿到：光标不再被锁在缩放箭头上、更新过程不再闪终端、文件标签按类型细分（403 个扩展名）、安装版卸载时可选择是否保留本地数据，以及更早的设置分组感重做、列表里 Ctrl+C 复制完整原文、预览完整显示长文、大小单位到 GB / TB 等。构建脚本里的 PyInstaller 固定为 6.22.3。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
