v3.2.0：列表显示修复（格式 / 尺寸 / 大小 三列不再被截断成 `…`，改为按内容自适应并与表头居中；大小单位补齐到 GB / TB），复制文件时不再多出一条"文件名文本"，窗口缩放更顺滑且背景保持比例铺满，退出时不再残留临时目录，英文界面不再夹中文、中文界面去掉重复英文对照。同时让检查更新更稳：GitHub API 被限流或网络受限时自动改用 Releases 页面取版本号，境外网络也能正常更新。

从 v3.0.0 及更早版本升级还会一并获得 v3.1.0 的全部更新：「全部」聚合分类与类型标签、可选接管 Win+V 打开 / 收起本工具、复制与粘贴提示音（每次都响）、手机端上传图片 / 文件与读取手机剪贴板、统一的卡片式弹窗，以及大文本正文单独存盘等稳定性改进。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
