v3.2.2：修掉"应用内更新完成后报错、必须退出重开"的问题。在 3.2.0 里点「检查更新」升到 3.2.1，替换完成后新版本会弹出 `Security validation failure: unexpected name of application's home directory!`，关掉应用再打开才正常。

原因：新版 PyInstaller（6.22.3）给 onefile 程序加了一道安全校验（安全公告 GHSA-9fxf-4qw3-ghmr）——如果环境里带着"上一级 onefile 进程的临时目录"（`_PYI_APPLICATION_HOME_DIR` 等），它会要求那个目录名必须是 `_MEI` + 8 位十六进制 PID；而我们的更新流程用一个批处理替换文件再启动新版本，这个批处理继承了旧进程的那套变量（旧进程此时已经退出），新版本校验不过就自己退出了。现在更新器会在启动新版本前把这几个变量清干净（批处理里 `set` 清一次，启动时再传一份过滤过的环境），更新完成即自动打开，不再报错；手动重新打开的方式也照常可用。

本次也把构建脚本里的 PyInstaller 固定为 6.22.3，避免以后又因为自动升级到最新版而引入新的启动校验或行为变化。

如果你还在 v3.2.0 或更早版本，升级还会一并拿到：设置界面的分组感重做（模块标题升为 13px 主色、条目降一档、组间留白 15px > 组内 7px）、列表里 Ctrl+C 复制完整原文（不再把带 ⏎、被截断的预览文字复制出去，也不会再因此多出一条重复记录）、预览面板完整显示长文、列表列宽自适应与大小单位补齐到 GB / TB、复制文件不再多出一条"文件名文本"、窗口缩放更顺滑且背景保持比例铺满、退出不残留临时目录、中英文界面不再互相夹杂。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
