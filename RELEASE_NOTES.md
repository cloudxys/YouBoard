v3.3.4：修掉「更新失败把主程序删掉」这个致命问题，并修好 AI 配置里自定义服务商丢失、AI 服务卡片截图带背景与不居中的问题。

【新：密库主密码】以前密库和剪贴板历史共用同一把自动生成的 `youboard.key`，打开密库不需要任何密码——防得住"文件被单独拷走 / 云盘泄露"，防不住"能坐到这台电脑前的人"，和 KeePass 那种"就算电脑开着也要主密码才能看"的心智预期有落差。现在密库可以单独设一把**主密码**：用 PBKDF2-HMAC-SHA256（20 万次迭代 + 随机盐）从主密码派生一把独立密钥，密库清单**和里面的图片**都改用它加密，和 `youboard.key` 完全脱钩；打开密库先看到锁屏页（用的是 `res/suo.png` 挂锁图标），输对了才显示内容；**闲置 5 分钟或关掉窗口自动锁定**，锁定后连内存里的明文也一起丢掉、解密出来的图片副本一并清干净。密码框右侧带「显示 / 隐藏」小眼睛，输入时可以随时看明文。解锁之后右下角有三个入口：**「关闭密码」**（一键退回"直接就能打开"，随时能再设回来，不用再输一次密码）、「主密码…」（设置 / 修改 / 取消，改和取消要先验证当前密码）、「锁定」（立刻锁上）。主密码只在本机校验、不存任何地方，忘了就用锁屏页的"清空密库并重设"。本地加密的边界写在 README 隐私一节：防的是文件被拷走、云盘泄露、旁人翻程序，挡不住本机上正在运行的恶意程序。另外：密库锁定状态下**绝不写盘**（内存里没有内容，写下去等于清空）；从主界面「加入密库…」时如果密库锁着，会先弹一个就地解锁的小卡片。

【修复：卡片和打开它的窗口对不上】从上一条反馈里量出来：卡片以前一律按**主窗口**居中，而设置窗口常常被屏幕边挤得不在主窗口正中——所以"历史保留策略"这类从设置里打开的卡片，看着就和设置窗口错开（你截图里差 40px 左右，屏幕小的时候更明显）。现在卡片跟**真正打开它的那个窗口**对齐：设置里开的卡片就落在设置窗口正中间，主界面开的还是落在主窗口中间（窗口不可见时退回主窗口 / 当前屏幕）。

【修复：密库里的确认卡片也不居中 + 自动锁定可以自己设】① 「关闭密码」这类从密库窗口弹出来的确认卡片，以前哪怕改了居中规则也还是偏下：它沿用了"卡片弹层"那套居中，但自己的高度是按内容首选值算的，而结果卡片本身设了最小高度（330），Qt 把窗口顶到 330 之后位置没跟着更新 —— 看着就比正中心低 25px 左右。现在算尺寸时会把卡片的最小高度一起算进去，实测确认卡片与密库窗口中心完全重合（389,359 vs 389,359）；更新卡片那边同步修了同一处（内容摊开后窗口尺寸变了会重新回到正中间，用户拖过的位置仍然保留）。② 闲置自动锁定不再是写死的 5 分钟：密库 →「主密码…」里有 **1 分钟 / 5 分钟 / 15 分钟 / 30 分钟 / 不自动锁定** 五档可选，选完立刻生效并记住；只要新密码留空就只保存这个时间，不用再输一遍当前密码。门禁里加了对应断言（档位齐全、选中生效、留空只存时间、改密码仍需当前密码、"不自动锁定"会真的停掉计时器、设置写进配置、确认卡片落在密库窗口正中间）。

【重要：更新失败不会再删掉主程序】有人更新 3.3.2 → 新版时更新失败，结果 `YouBoard.exe` 直接消失，目录里只剩一个 `YouBoard.exe.update_failed`。查下来是替换脚本的回滚路径有三个洞：① 新版**一启动就把 `.bak` 备份删掉**，而脚本要等 20 秒才判断"新版到底活了没"——如果新版在这 20 秒里崩掉 / 被关掉 / 被判失败，回滚时备份早就没了；② 脚本回滚时**先删主程序再还原备份**，备份不在就等于把用户唯一的主程序删光；③ 等待循环用"`.bak` 存在"当成"这次备份好了"，上一轮残留的备份会让它在主程序还被占用时就往下走。本版：备份改到**确认新版真的活着之后**才删（新版启动后会写一个 `.update_ok` 凭据，脚本看到才算成功，最多等 60 秒，写不出凭据时仍用进程名兜底）；回滚**先确认备份可用**，备份不在就**保留现有主程序**并提示重新安装，绝不把目录清空；开跑前先清掉上一轮残留的 `.bak`（并在主程序被强行挪走时先用备份自愈）；回滚过程也改成"先把装进去的挪到一边"，搬不回来还能放回去。这一条已经用真实 `cmd.exe` 端到端跑过 5 个场景：更新成功、校验不过回滚、没有备份、残留备份、备份被提前删掉+回滚——**每种失败路径下主程序都还在**；旧脚本在同样的"备份被提前删掉"时序下确实会把主程序删光（已能稳定复现，作为回归用例留在了门禁里）。

【修复：AI 配置里「自定义」服务商填的东西会丢】AI 服务里配好「自定义」的接口地址和模型、保存后再进来，点一下别的服务商、再点回「自定义」，内容就全空了（切服务商时被"该服务商的默认值"直接覆盖，而自定义的默认值是空的）。现在每个服务商各自记住你填过的内容：切走再切回来原样还原，没填过的才用默认值。

【修复：AI 服务卡片抓窗口带一圈背景 / 卡片不居中】AI 服务卡片的窗口宽度以前是按 `sizeHint` 算的，而卡片里的说明文字是自动换行的——`sizeHint` 会按"整段排成一行"算出八百多像素，窗口跟着变那么宽，卡片本身还是 520，于是窗口右边多出一条背景（截图工具按窗口矩形抓图就会把它一起截进来，用户反馈"识别截图给我截宽了"）。现在窗口宽度按**内容真正需要的最小宽度**算，并且每次都把卡片钉成窗口这么大：**窗口 = 卡片**，抓窗口就是抓卡片本身（顺带消掉了卡片比窗口内容窄时按钮被挤的问题）。另外内容变化导致窗口尺寸变化时，只要你没手动拖过，卡片会**重新回到宿主正中间**，不会越变越偏。

【其它】回归门禁扩到 **383 项**，全部通过（新增：换包脚本 6 项静态断言 + 5 个真实 `cmd.exe` 场景、AI 自定义服务商取值、AI 服务卡片窗口贴合与内容变化后居中、四个此前没被门禁覆盖的卡片弹层）。顺手补上两个门禁自身的坑：① 核心用例以前只把"历史文件"指到临时目录，快照 / 配置 / 密库仍然指向真实数据目录，`clear()` 之类会把用户真实的 `.youboard_snapshots.json` 清成空表（本机实测清没了 8.5MB 快照，已从副本还原）；现在三处用例的数据路径全部指到临时目录，并且每轮门禁结束都会核对真实数据文件有没有被改动（`.youboard.json` 除外——项目目录里正开着程序时它本来就会变）。② 无头（offscreen）测试带着几 MB 的快照文件跑，Qt 在对象销毁阶段会偶发 `0xC0000005`，改成在项目副本里先把快照清空再跑。

---

v3.3.3：卡片弹层可以按住拖动；卡片内容变化时窗口自动跟随，不再把内容裁掉。

【卡片可以拖动了】所有卡片式弹层（历史保留策略 / 自定义皮肤 / 编辑标签 / AI 服务 / AI 结果 / 浏览器扩展配置 / 密库新增与重命名 / 更新卡片与更新结果卡片）现在都能**按住卡片空白处或标题拖动**到任意位置；拖过之后，窗口再因为内容变化调整尺寸时也**不会把你拖走的位置拽回去**。按钮、开关、输入框这些控件照常点击，不受影响。

【修复：卡片内容变化时被裁掉】「历史保留策略」点「自定义」会多出一行，以前卡片会变高但**窗口没跟着长**，结果上半截被裁在窗口外面（只剩半行按钮）。原因是窗口尺寸只在显示时算了一次，而且卡片还被上一次设的"最大高度"卡着。现在：每次尺寸都是按卡片**当前内容的首选尺寸**算，内容一变就立刻把窗口跟上（另有 300ms 兜底），卡片上限也一起放开——实测 275 → 314 → 275，全程窗口严格贴合卡片，不再裁切、也不会先闪一下被挤扁的样子。

【内存占用大幅下降】常驻后台 / 长时间空闲时，程序会主动把工作集还给系统（先 GC，再用 `SetProcessWorkingSetSize` + `EmptyWorkingSet`；页面只是移到系统的 standby 列表，需要时会立刻映射回来，不会变慢）。触发时机：启动 8 秒后、收进托盘 2.5 秒后各一次，之后每分钟检查——窗口隐藏 / 最小化或空闲超过 2 分钟就回收。实测（冻结版 EXE，任务管理器"内存"列的口径 = 专用工作集）：**收进托盘 80 秒后 5.1 MB**（改动前约 30 MB）、**运行中 5.1 MB**（改动前约 58 MB），启动器父进程另占 1.2 MB。

---

v3.3.2：密库改成「移入 / 移出」——放进密库的内容不再留在剪贴板历史里，右键可以移回去，并按当初的时间归位。

【密库：移入 / 移出】以前「加入密库」只是复制一份过去，剪贴板历史里还留着同一条（密钥等于没藏住，还会被手机传输 / 云同步带出去）。现在保存进密库时会把它**从剪贴板历史里移走**（记录本身不丢，连同**原来的时间戳、标签、收藏、置顶**一起存进密库）；在密库列表里右键 →「移出密库…」，它会**按当初复制的时间原样回到历史**（在按时间排序的列表里回到原来的位置），标签与收藏也一并还原。密库条目本身来自哪条历史记录、什么时候复制的，都记在条目里，所以来去都在原位。

【密库列表：右键菜单 + AI 处理】密库列表现在有和历史列表同一套右键菜单：复制（内容 / 图片 / 文件）、导出为 .txt…、打开（图片查看器 / 文件定位）、复制路径、打开所在文件夹、编辑、重命名、**AI 处理 ▸**、删除；只有「加入密库」换成了「移出密库」。**密库里的内容同样能用 AI**：文本 / 网址是总结 / 翻译 / 改写 / 提取要点 / 自定义提示 / 和 AI 聊聊，图片走视觉模型（描述图片 / 提取文字 / 翻译文字），文件走清单分析——动作与非密库完全一致；AI 结果「另存为新条」会存进密库、「替换本条」会更新这条密库记录（不会串到剪贴板历史里）。

---

v3.3.1：新增「密库」（手动存放私密内容），修好标签编辑没反应的问题，并给自动更新加上完整性校验与自动回滚（修掉一个会让主程序被换成打不开的残缺文件的致命 bug）。

【重要：自动更新不再会把软件弄坏】有人遇到过"更新之后主程序双击报文件损坏、彻底打不开"——查下来是两个叠加的缺陷：① 多线程分段下载会先把文件按总大小预填成零字节，只要有一段分片失败也当成下载成功（于是拿到一个 87% 全是零的假 EXE）；② 替换脚本是"先删掉旧主程序、再把新文件改名顶上"，中间任何一步失败就没有退路了。本版做了四件事：下载器现在按分片逐一核对边界、要求状态码为 206、分片长度必须与请求一致，并且必须"整段无缝覆盖"才算下载成功；下载完统一做完整性校验（体积、MZ 头、PE 头偏移与签名、PyInstaller 归档标记、有发布摘要时再比对 SHA256），不通过就自动换源重下；安装前**先把旧主程序改名备份**（不再删除），新文件搬进去后再用 certutil 校验一次 SHA256，验不过就**自动回滚到更新前的版本**并提示你原因；下一次成功启动后才会删掉备份。上面的校验对真实构建和"87% 零字节"的损坏文件都实测过。

【密库】顶栏新增挂锁按钮，打开就是一个独立窗口：把你选中的私密内容**由你主动放进去**（程序不会自动往里写任何东西）。和主界面一样分**全部 / 文本 / 图片 / 文件 / 网址**五类，每条内容**名称可以不填**——不填就直接按内容显示（长内容自动截断），想记名字再填。支持搜索、新增、编辑、重命名、复制、打开所在位置、删除；**列表里右键任意记录 →「加入密库…」四类都能存**：文本和网址存正文，图片会把文件复制进密库目录，文件记路径（源记录不受影响）。复制走的是应用内复制标记，**不会**被剪贴板监控记进历史。数据单独存在 youboard_vault.json（Fernet 加密 + 原子写入，图片放在 vault_files/），不参与手机传输、云同步和历史快照；旧版密库（名称/账号/密码那套表单）里的内容会在首次打开时自动迁移过来，不会丢。提示：密库目前与本机历史使用同一把本地密钥（youboard.key），安全性等同历史加密，但**没有**额外的主密码锁；需要的话下一版可以加。

【右键：加入密库】在列表里右键一条**文本 / 网址**记录 →「加入密库…」，会把这条内容直接带进密库的新增弹层（密码 / 密钥字段预填内容，名称默认取这条记录的标签或内容开头，可自行修改），确认即存。源记录不受影响，两边是独立数据。

【密库输入框：中文圆角右键菜单 + 一键复制内容】编辑密库内容时，输入框右键以前弹的是 Qt 自带的英文菜单（还是一圈系统直角方框），现在换成应用自己的圆角菜单：剪切 / 复制 / 粘贴 / 全选。「内容」旁边也加了**「复制内容」**按钮——长内容不用手动全选，点一下就复制走（复制走的是应用内复制标记，不会被剪贴板历史再收一条）。

【密库弹层：内容框对齐】「内容」多行输入框以前没被弹层样式表覆盖，落到全局样式上（透明底、无边框、Consolas 字体），于是既没有框、文字起点也和「名称」差几像素。现在两个框共用同一套输入框样式（底色 / 边框 / 圆角 / 内边距 / 字体一致），文字起始位置实测完全对齐（同一像素列）。

【更新：启动自检 + 维护手册】替换脚本在原"备份 + SHA256 校验 + 回滚"之上又加了一步：装好后等一段时间确认新进程真的活着，起不来就自动回滚到更新前的版本并提示原因。另外新增 MAINTENANCE.md，把版本号唯一来源、门禁、打包步骤、更新链路的硬约束和数据红线都写成了一页纸，方便以后长期维护。

【密库状态栏文案】筛选某个分类时，以前显示「网址 1 / 5」，容易读成"有 5 个网址"（那个 5 其实是全部条数）。现在改成「网址：1 条 · 全部 5 条」，搜索时显示「筛选出 N 条 · 全部 M 条」。

【浏览器扩展：中英双语】扩展现在跟随浏览器语言显示：界面文案（面板两个页签、按钮、状态提示、右键菜单、设置页、扩展名称与简介）全部走 `_locales/`（英文为默认，中文为 zh_CN）——中文环境看到中文，其它语言自动回落英文。商店列表方面，STORE_LISTING.md 里补了「Add or remove a language」的操作步骤和整套中英文案，在 Partner Center 加上简体中文那一栏，商店就会按访问者语言显示对应介绍。打包脚本已把 `_locales/` 一起收进 zip，重新上传即可。

【修复：扩展上架包被拒（简介超长）】Edge 校验要求 manifest 里的简介 ≤132 字符，上一版英文简介写了 185 字符，所以上传即报 "Description in locale en is too long"。中英文简介都已精简并在门禁里加了长度断言（≤132），重新打包的 zip 可直接上传。

【修复：浏览器扩展连不上桌面端（Failed to fetch）】两个原因：① 本机桥的开关以前只在点「保存」时才真正启动，拨了开关但没保存时开关看着是开的、状态却写「未运行」，扩展自然连不上；② 即使启动失败（端口被占等）也会被静默吞掉，界面上只显示「未运行」，无从排查。现在拨开关**立即启动/停止**（并立刻写配置），状态行实时显示「运行中：127.0.0.1:端口 · 扩展上次连接 时间」；启动失败则把具体原因（例如 `OSError: [WinError 10048]`）直接显示出来。门禁里新增：开关一开就能 ping 通 `/api/ping`、关掉即停、失败原因必须能显示。

【修复：卡片弹层截图"太大 / 全屏"】以前卡片弹层（编辑标签、历史保留策略、浏览器扩展配置、AI 服务…）的窗口会被撑成和主窗口一样大——窗口最大化时等于整屏；后来虽然改成只包卡片，但卡片四周还留着 16~48px 的透明边距，截图工具按**窗口矩形**截出来仍会把卡片周围的界面带进来（用户实测反馈）。

现在做法：弹层显示时**先让卡片按自身内容摊开到自己的尺寸**，随后窗口**严格收紧到卡片的实际尺寸**（不留边距、不压卡片）——历史保留策略 520×261、浏览器扩展 520×275、编辑标签 520×235、更新结果 600×360、更新卡片 600×391，窗口矩形 = 卡片。**卡片自身的尺寸与排版和以前完全一致**（中途有个版本因为读到"滞后一拍"的高度把更新结果卡片压小了 78px、导致底部"确定"被截断，本版已修正，并加了断言：每个弹层"窗口必须贴合卡片（误差 ≤4px）"）。

【浏览器扩展 1.0.1】三处更新：① **修复桌面端历史时间显示错误**——本机桥回传的是 ISO 时间字符串（`2026-09-22T15:20:41`），面板却按毫秒数解析（`Number()` → NaN → 回落到"现在"），于是每条都显示成当前时间；现在两种格式都能正确格式化。② 简介精简到商店要求的 132 字符以内（上一版被拒的原因）。③ 新增 `tools/verify_extension.js`：用假的 chrome API 在 Node 里把扩展逻辑真跑一遍（捕获/去重/条数上限/站点排除/搜索/删除/收藏/清空/推送桌面端/拉取历史/复制/右键菜单中英文案/面板时间与徽标），共 **31 项断言**，已接进 CI 门禁，扩展以后每次改动都会被这些用例看住。

【更新弹窗也改成"只包卡片"】"已是最新版本"这类更新结果卡片和下载进度卡片，之前窗口仍是主窗口大小（最大化为整屏），截图工具识别不到/抓成整屏；现在和别的卡片弹层一样，窗口只包住卡片（实测：更新结果卡片 648×408，主窗口 1200×760）。

【修复：编辑标签没有用 / 弹层里回车"把内容弄丢"】旧版有两处回车问题：① 打完标签直接点「保存」，输入框里那串文字会被丢掉；② 按回车反而会触发默认按钮（取消），弹层被关掉、刚输入的内容全没了。现在：回车 = 把标签加进来（弹层不关），点「保存」= 存下（输入框里没回车的字也一并收下）。密库的新增 / 编辑弹层里，回车 = 直接保存。

【桌面小组件：右键关闭】在小组件上点右键 → 「关闭小组件」即可收起，不用再跑到设置里去关（想重新显示：设置 → 桌面小组件）。右键同时不会被当作左键的"点击条目复制"，不会误复制内容；这个菜单改成了紧凑规格（约 125×39 → 91×27），和小部件本身更搭。

【其它】回归门禁扩到 260 项，全部通过（含更新完整性校验、真实分片下载、残缺分片必须被拒、密库四类与可选名称、旧密库数据迁移、标签保存、回车语义、右键加入密库、小组件右键等用例）。安装包、便携版与 macOS 产物版本号统一到 3.3.1。

---

v3.3.0：浏览器扩展（伴侣）+ 桌面端本机桥（只连 127.0.0.1）。

【浏览器扩展（伴侣）】新增 edge_extension/（Manifest V3，版本 1.0.0）：在网页里 Ctrl+C 复制的内容（文本 / 链接 / 图片）会被记下并按网站标好来源。点扩展图标打开面板，两个页签——「网页捕获」（只存浏览器本地）与「YouBoard 历史」（从桌面端读取、可搜索）；单击条目=复制，双击=直接插进当前网页的输入框；右键菜单可存选中内容 / 本页链接 / 这张图片；支持按网站设置不捕获。扩展完全本地：只与本机 127.0.0.1 上的 YouBoard 通信，不向任何服务器发送数据（隐私政策见仓库）。

【桌面端：本机桥】新增 youboard_bridge.py —— 一个只监听 127.0.0.1 的本地服务（随机令牌鉴权，退出自动关闭），提供查历史（支持搜索、图片缩略图）、存一条（文本 / 网址 / 图片）、把某条复制到系统剪贴板（会标记 self-copy，不会被再记一条）、收藏等接口。设置里新增「浏览器扩展」入口：设置页只留一行摘要 +「配置」，点开是独立卡片弹层（与「AI 服务」同一套风格），里面有开关、端口、令牌、一键复制连接信息、运行状态与「扩展上次连接」时间；端口被占用会自动换一个空闲端口。

【怎么装扩展】Edge：地址栏输入 edge://extensions/ →打开「开发人员模式」→「加载解压缩的扩展」→ 选择 edge_extension 目录；Chrome 同理（chrome://extensions/）。装好后在桌面版 设置 → 浏览器扩展 → 复制连接信息，粘到扩展的「设置」里即可。上架 Edge 加载项商店的材料已备好（商店文案、逐条权限理由、隐私政策、打包脚本），审核中。

【文档】README 更新（新增「浏览器扩展」说明与「支持作者」），并新增 COMPARISON.md：与 Ditto / CopyQ / ClipClip / Paste / Raycast 的逐项对比，含我们自己的短板与路线图。

【其它】本版桌面端的日常使用没有变化；回归门禁扩到 186 项（新增桥接 11 项接口用例 + 界面/开关 6 项）。如果你还在 3.2.9 或更早版本，升级还会一并拿到：图片与文件分类接入 AI（含图片视觉分析）、标签/收藏、历史保留策略、关闭窗口行为开关、桌面小组件不被弹层遮挡等。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
