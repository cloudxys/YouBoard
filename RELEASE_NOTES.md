v3.2.7：两个新功能（标签 / 收藏、AI 就地处理）+ 设置与小组件体验修复。

【标签 / 收藏】纯本地功能，不用注册、不联网。右键任意记录即可打标签、加星收藏；收藏过的记录在列表里内容前会多一颗主题色五角星。标签页上方多出一排小胶囊——「收藏 N」和「#标签 N」，点一下只看这一类，再点取消，能和关键词搜索、「文件」细分筛选叠加使用。搜索框里直接输标签名也能找到对应记录。标签与收藏存在本机历史里，重复复制同一段内容、导入其它 YouBoard 数据、云同步合并都会保留。

【AI 就地处理】选中一条记录 → 右键「AI 处理 ▸」，或按 Ctrl+I：总结要点 / 翻译 / 改写润色 / 提取关键信息 / 自定义提示。结果流式显示（能看着字出来），可以复制、另存为新条，或直接替换本条（替换时标签与收藏保留）。自带 API Key，支持 DeepSeek / 通义千问 / 智谱 GLM / OpenAI / Ollama 本地以及任意 OpenAI 兼容接口；设置里填好接口地址与模型后，「测试连接」可当场验证。只发送你选中的那一条（不带其它历史），单次最多 2.4 万字符输入、约 1200 token 输出，Key 在本机加密保存、不写日志。

【设置与小组件】AI 服务收进卡片弹层（和「历史保留策略」同款），设置页只留一行摘要 +「配置」，不再被这一项拉长；弹层里的输入框、数字框直角框已去掉。打开设置（以及 AI 配置、历史保留策略等弹层）时，桌面小组件依然可以点、可以复制——之前是应用级模态，会把整个程序的窗口禁用；现在改为窗口级模态，只挡住宿主窗口。小组件本身依旧不抢焦点（点它不会打断前台应用或游戏）、始终置顶、位置尺寸自动记忆。

【界面修复】「文件」分类的细分胶囊在宽窗口下以前会被均匀摊开（1500px 时相邻间距 37px），现在恒定 6px、不再漂移；AI 服务弹层的中文标签不再被裁成半个字。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
