v3.2.7：新增「标签 / 收藏」——纯本地功能，不用注册、不联网，当天就能用。

收藏：右键任意一条记录 →「加入收藏」（或在右侧预览顶上点星标）。列表里收藏过的记录，内容前面会多一颗主题色五角星。

标签：右键「编辑标签…」给一条记录打标签，输入后回车即可，历史标签点一下就能勾上；右侧预览顶上也能进。一次选中多条时是「批量添加标签」，已经有的标签会显示成「已有」，不会重复加。

筛选：标签页上方多出一排小胶囊——「收藏 N」和「#标签 N」（带数量，平时不占地方）。点一下只看这一类，再点一次取消；能和关键词搜索、「文件」细分筛选叠加使用。搜索框里直接输标签名，也能找到打了这条标签的记录。

数据不会丢：标签与收藏就存在本机历史文件里，重复复制同一段内容、导入其它 YouBoard 的数据、云同步合并，都会保留。单个标签最长 24 个字符，一条记录最多 20 个标签。

快捷键：设置 → 动作快捷键新增「收藏 / 取消收藏」（默认 Ctrl+D），和别的动作一样可以自己改。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
