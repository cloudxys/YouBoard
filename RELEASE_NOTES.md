v3.2.9：AI 覆盖到「图片」和「文件」两个分类（图片真的让模型看图），设置里新增「关闭窗口行为」开关。

【图片分类的 AI】右键「AI 处理 ▸」现在会按分类给不同动作：图片是「描述这张图片 / 提取图中文字 / 翻译图中文字 / 自定义提示 / 和 AI 聊聊」。图片会按多模态格式发给模型（真正的视觉分析，不是文件名猜测）：发送前先压到长边 1280 的 JPEG（质量 82），一张 2400×1600 的图压完只有几 KB，状态栏会写「已按 1280×853 压缩后发送」。注意：图片分析需要支持视觉的模型，例如 deepseek-v4-flash-vision-exp、gpt-5.6、qwen3.5-omni-plus、glm-5v 或本地视觉模型，在 AI 服务里把模型换成视觉款即可。

【文件分类的 AI】文件是「总结文件清单 / 给整理建议 / 自定义提示 / 和 AI 聊聊」。模型看不到文件本体，所以发送的是清单（文件名 / 格式 / 大小 / 合计），用来判断这批文件大概是什么、有没有重复或体积异常、该怎么归档。

【关闭窗口行为】设置 → 通用 的第一项新增开关，默认「直接退出」（保持原来的行为，不做成必须缩后台的软件）。打开后点右上角 ✕ 只是把窗口收进托盘，并给一次气泡提示：复制记录、快捷键、手机传输都继续工作；要真正退出仍走托盘右键 → 退出。开关左边会显示当前取值（收进托盘 / 直接退出），一眼能看出开与关分别是什么。

【模型名左右分开】AI 服务里的「模型」改成两个框：左边是接口真正使用的模型 id（如 deepseek-flash），右边是显示名，可以随便改（如 DeepSeek-V4.1-Flash）。显示名只用于界面展示（AI 弹层副标题、设置里的摘要），请求里发出去的始终是左边的真实 id。

【小修】AI 结果弹层里那行说明（生成完成 / 图片压缩提示等）往上贴得太近，现已下移并与结果区拉开 24px，不会再挤在一起。

【导入数据修正】导入别的 YouBoard 数据时：本机 700 条 + 导入 300 条会**合并**成合并后的总数（不会把原来的覆盖掉），完全重复的记录只保留一条，并按时间重新排好（置顶记录始终排在前面）。另外修掉一个重复的边角情况：当重复的那条正好是本机的**置顶**记录时，以前会在「置顶」和普通列表里各留一份，现在只保留置顶的那一份。

支持 Windows（安装版 / 便携版）与 macOS（Apple Silicon / Intel）。
